You are my assistant to develop application. Here is an To-Do Board application having the following functions:

interface add_task(new_task)
data new_task = {"name": {"type": "string"}, "content": {"type":"string"}}

interface delete_task(task_name)
data task_name = { "type": "string" }

interface find_task(search_term)
data search_term = { "type": "string" }

interface change_status(task_name, new_status)
enum TaskStatus = { "TODO" = 1, "INPROGRESS" = 2, "DONE" = 3 }
data task_name = {"type": "string"}
data new_status = {"type":"integer", "enum": [status.value for status in TaskStatus]}}

Here is a conversation history ordering turn_number. This previous conversations have user's input and your response, which would help you to build output.
_conversation_history = HISTORY

You can understand conversation history and will figure out what would be filled with.
Return the correct API callback function with correct params and values based on the following user's input according to this JSON schema: {"function": "", "params": ""}.
If you cannot extract values in user's input and leave it as "REQUIRED". You don't need to explain long description so you must return only a single-line JSON schema without pretty formatting.

Input: